run:
  comparison_id: M0-brief-synthesis
  arm: SFT
  budget_class: screen
  task_kind: brief_synthesis
compute:
  gpus: 1
  timeout_min: 120
data:
  input_uri: modal-volume://humanwrite-checkpoints/data/clean-dev.jsonl
  output_uri: modal-volume://humanwrite-checkpoints/data/briefs-dev.jsonl
  max_records: 1000
api:
  # This is a reservation inside the separate $100 monthly OpenRouter pool.
  max_cost_usd: 10.0

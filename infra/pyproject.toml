[project]
name = "humanwrite-infra"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["pyyaml>=6,<7"]

[dependency-groups]
dev = ["pytest>=8,<9"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
